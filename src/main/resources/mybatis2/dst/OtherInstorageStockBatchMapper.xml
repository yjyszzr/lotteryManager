<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OtherInstorageStockBatchMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_other_instorage_stock_batch
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		other_instorage_stock_batch_id,	
		other_instorage_code,	
		other_instorage_detail_id,	
		sku_id,	
		sku_name,	
		sku_encode,	
		batch_code,	
		quantity,	
		spec,	
		unit_name
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{other_instorage_code},	
        #{other_instorage_detail_id},	
        #{sku_id},	
        #{sku_name},	
        #{sku_encode},	
        #{batch_code},	
        #{quantity},	
        #{spec},	
        #{unit_name}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        other_instorage_code,	
		        other_instorage_detail_id,	
		        sku_id,	
		        sku_name,	
		        sku_encode,	
		        batch_code,	
		        quantity,	
		        spec,	
		        unit_name	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			other_instorage_stock_batch_id = #{other_instorage_stock_batch_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			other_instorage_stock_batch_id = #{other_instorage_stock_batch_id},
			other_instorage_code = #{other_instorage_code},
			other_instorage_detail_id = #{other_instorage_detail_id},
			sku_id = #{sku_id},
			sku_name = #{sku_name},
			sku_encode = #{sku_encode},
			batch_code = #{batch_code},
			quantity = #{quantity},
			spec = #{spec},
			unit_name = #{unit_name},
		other_instorage_stock_batch_id = other_instorage_stock_batch_id
		where 
		other_instorage_stock_batch_id = #{other_instorage_stock_batch_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			other_instorage_stock_batch_id = #{other_instorage_stock_batch_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
	
		<if test="pd.other_instorage_code != null and pd.other_instorage_code !=''">
			and other_instorage_code = #{other_instorage_code}
		</if>
		<if test="pd.other_instorage_detail_id != null and pd.other_instorage_detail_id !=''">
			and other_instorage_detail_id = #{other_instorage_detail_id}
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		
		<include refid="tableName"></include>
		where 1=1
	
		<if test="other_instorage_code != null and other_instorage_code !=''">
			and other_instorage_code = #{other_instorage_code}
		</if>
		<if test="other_instorage_detail_id != null and other_instorage_detail_id !=''">
			and other_instorage_detail_id = #{other_instorage_detail_id}
		</if>
		<if test="batch_code != null and batch_code !=''">
			and batch_code = #{batch_code}
		</if>
	</select>
	<!-- 查询批次号，用于校验批次号是否非法 -->
	<select id="listBatch" parameterType="pd" resultType="pd">
		SELECT * FROM batch_generate where batch_no = #{batch_no} and latest_no >=#{latest_no}; <!--  69709772512901710115-->
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			other_instorage_stock_batch_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>