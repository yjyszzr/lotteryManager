<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AllocationDetailMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_allocation_detail
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		allocation_code,	
		sku_id,	
		sku_name,
		sku_encode,
		pre_quantity,	
		quantity,	
		status,	
		createby,	
		create_time,	
		updateby,	
		update_time,	
		spec,
		allocation_detail_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{allocation_code},	
        #{sku_id},	
        #{sku_name},	
        #{sku_encode},	
        #{pre_quantity},	
        #{quantity},	
        #{status},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time},	
        #{spec}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        allocation_code,	
		        sku_id,	
		        sku_name,
		        sku_encode,
		        pre_quantity,	
		        quantity,	
		        status,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time,	
		        spec
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			allocation_detail_id = #{allocation_detail_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			<!-- allocation_code = #{allocation_code},
			sku_id = #{sku_id},
			sku_name = #{sku_name},
			sku_encode = #{sku_encode}, -->
			pre_quantity = #{pre_quantity},
			<!-- quantity = #{quantity},
			status = #{status},
			createby = #{createby},
			create_time = #{create_time}, -->
			updateby = #{updateby},
			update_time = #{update_time},
			<!-- spec = #{spec}, -->
		allocation_detail_id = allocation_detail_id
		where 
		allocation_detail_id = #{allocation_detail_id}
	</update>
	
	
	<!-- 修改 -->
	<update id="updateQuantity" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			quantity = #{quantity},
			updateby = #{updateby},
			update_time = NOW()
		where 
		allocation_detail_id = #{allocation_detail_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			allocation_detail_id = #{allocation_detail_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		    pad.allocation_code,	
			pad.sku_id,	
			pad.sku_name,
			pad.sku_encode en,
			pad.pre_quantity,	
			pad.quantity,	
			pad.status,	
			pad.createby,	
			pad.create_time,	
			pad.updateby,	
			pad.update_time,	
			pad.allocation_detail_id,
			g.unit_name,
			g.spec spec,
			g.isopen_batch,
			g.attr_cate,
			gs.sku_encode sku_encode
		from pgt_allocation_detail pad
		left join szy_goods_sku_6 gs on pad.sku_id = gs.sku_id
		left join szy_goods_6 g on g.goods_id = gs.goods_id
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		<if test="pd.allocation_code!= null and pd.allocation_code != ''"><!-- 调拨编号检索 -->
			and
				(
					pad.allocation_code = #{pd.allocation_code}
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			allocation_detail_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 列表 -->
	<select id="listByCode" parameterType="pd" resultType="pd">
		select
		    pad.allocation_code,	
			pad.sku_id,	
			pad.sku_name,
			pad.sku_encode en,
			pad.pre_quantity,	
			pad.quantity,	
			pad.status,	
			pad.createby,	
			pad.create_time,	
			pad.updateby,	
			pad.update_time,	
			pad.allocation_detail_id,
			g.unit_name,
			g.spec spec,
			g.isopen_batch,
			g.attr_cate,
			gs.sku_encode sku_encode
		from pgt_allocation_detail pad
		left join szy_goods_sku_6 gs on pad.sku_id = gs.sku_id
		left join szy_goods_6 g on g.goods_id = gs.goods_id
		where pad.allocation_code = #{allocation_code}
	</select>
	
	<!--通过Code查询所有信息-->
	<select id="findByAllocationCode"  parameterType="pd" resultType="pd">
	
		SELECT d.sku_encode,d.sku_name,d.pre_quantity,d.quantity,d.spec
		FROM
		pgt_allocation_detail d
		LEFT JOIN
		pgt_allocation a 
		ON
		d.allocation_code = a.allocation_code
		WHERE 
		d.allocation_code = #{allocation_code}
		
	</select>
	<!-- fh313596790qq(青苔) -->
</mapper>