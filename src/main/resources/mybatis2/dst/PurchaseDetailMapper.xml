<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PurchaseDetailMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_purchase_detail
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		detail_id,	
		purchase_code,	
		sku_id,	
		sku_name,
		sku_encode,
		quantity,	
		unit,	
		spec,
		total_send_quantity,	
		total_sended_quantity,	
		good_quantity,	
		bad_quantity,	
		price,	
		tax_price,	
		tax_rate,	
		status,	
		pre_arrival_time,	
		createby,	
		create_time,	
		updateby,
		purchase_group_id,	
		update_time
		
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{detail_id},	
        #{purchase_code},	
        #{sku_id},
        #{sku_name},
        #{sku_encode},
        #{quantity},	
        #{unit},	
        #{spec},	
        #{total_send_quantity},	
        #{total_sended_quantity},	
        #{good_quantity},	
        #{bad_quantity},	
        #{price},	
        #{tax_price},	
        #{tax_rate},	
        #{status},	
        #{pre_arrival_time},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{purchase_group_id},	
        #{update_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        detail_id,	
		        purchase_code,	
		        sku_id,
		        sku_name,	
		        sku_encode,
		        quantity,	
		        unit,
		        spec,	
		        total_send_quantity,	
		        total_sended_quantity,	
		        good_quantity,	
		        bad_quantity,	
		        price,	
		        tax_price,	
		        tax_rate,	
		        status,	
		        pre_arrival_time,	
		        createby,	
		        create_time,	
		        updateby,	
		        purchase_group_id,	
		        update_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			detail_id = #{detail_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
		<if test="purchase_code != null and purchase_code != ''">
			purchase_code = #{purchase_code},
		</if>
			
			sku_id = #{sku_id},
			sku_name = #{sku_name},
			sku_encode = #{sku_encode},
			quantity = #{quantity},
			unit = #{unit},
			spec = #{spec},
			total_send_quantity = #{total_send_quantity},
			total_sended_quantity = #{total_sended_quantity},
			good_quantity = #{good_quantity},
			bad_quantity = #{bad_quantity},
			price = #{price},
			tax_price = #{tax_price},
			tax_rate = #{tax_rate},
			status = #{status},
			pre_arrival_time = #{pre_arrival_time},
			createby = #{createby},
			create_time = #{create_time},
			updateby = #{updateby},
			update_time = #{update_time},
			purchase_group_id = #{purchase_group_id},
		detail_id = detail_id
		where 
		detail_id = #{detail_id}
	</update>
	
	<update id="qualityCheck" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			good_quantity = good_quantity + #{good_quantity},
			bad_quantity = bad_quantity + #{bad_quantity}
		where 
		detail_id = #{detail_id}
	</update>
	<update id="updateQuantityForAdd" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
		<if test="total_send_quantity != null and total_send_quantity != ''">
		 <!-- total_send_quantity = total_send_quantity + #{total_send_quantity}, -->
		 total_send_quantity =  #{total_send_quantity},
		</if>
		<!-- 减少 -->
		<if test="total_send_quantity_sub != null and total_send_quantity_sub != ''">
		  total_send_quantity =  total_send_quantity - #{total_send_quantity_sub},
		
		</if>
		<!-- 已发货 -->
		<if test="total_sended_quantity != null and total_sended_quantity != ''">
			total_sended_quantity = total_sended_quantity + #{total_sended_quantity},
		</if>
			detail_id = #{detail_id}
		where 
		detail_id = #{detail_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			detail_id = #{detail_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		<if test="pd.purchase_code!= null and pd.purchase_code != ''"><!-- 关键词检索 -->
			and
				( 
				purchase_code = #{pd.purchase_code}
				
				)
		</if>
	</select>
	
	<!-- 查询待生产加工的生产明细 -->
	<select id="detaillistPage" parameterType="page" resultType="pd">
		select
			ppd.detail_id,	
			ppd.purchase_code,	
			ppd.sku_id,	
			ppd.sku_name,
			ppd.quantity,	
			ppd.unit,	
			ppd.total_send_quantity,	
			ppd.total_sended_quantity,	
			ppd.good_quantity,	
			ppd.bad_quantity,	
			ppd.price,	
			ppd.tax_price,	
			ppd.tax_rate,	
			ppd.status,	
			ppd.pre_arrival_time,	
			ppd.createby,	
			ppd.create_time,	
			ppd.updateby,	
			ppd.update_time
		from 
		pgt_purchase_detail ppd
		left join pgt_purchase pp on ppd.purchase_code = pp.purchase_code
		where 1=1
		<if test="pd.business_type!= null and pd.business_type != ''"><!-- 关键词检索 -->
			and
				( 
				pp.business_type = #{pd.business_type}
				
				)
		</if>
		<if test="pd.status!= null and pd.status != ''"><!-- 关键词检索 -->
			and
				( 
				pp.status = #{pd.status}
				
				)
		</if>
		<if test="pd.search_purchase_code!= null and pd.search_purchase_code != ''">
			and
				( 
				ppd.purchase_code LIKE CONCAT(CONCAT('%', #{pd.search_purchase_code}),'%')
				)
		</if>
		<if test="pd.search_sku_name!= null and pd.search_sku_name != ''">
			and
				( 
				ppd.sku_name = LIKE CONCAT(CONCAT('%', #{pd.search_sku_name}),'%')
				)
		</if>
		order by ppd.create_time desc
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		
		<if test="purchase_code!= null and purchase_code != ''"><!-- 关键词检索 -->
			and
				( 
				purchase_code = #{purchase_code}
				
				)
		</if>
		order by create_time desc;

	</select>
	
	<!-- 根据条件查询 -->
	<select id="listByParam" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="purchase_code!= null and purchase_code != ''">
			and purchase_code = #{purchase_code}  
		</if>
		<if test="sku_id!= null and sku_id != ''">
			and sku_id = #{sku_id}
		</if>
		order by create_time desc;
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			detail_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	
	<update id="updateBadQuantityForAdd" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
		<if test="bad_quantity != null and bad_quantity != ''">
		 bad_quantity = bad_quantity + #{bad_quantity}
		</if>
		where 
		detail_id = #{detail_id}
	</update>
	
	<!-- fh313596790qq(青苔) -->
</mapper>