<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OtherInstorageMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_other_instorage
	</sql>
	
	<!-- 字段 -->  
	<sql id="Field">
		other_instorage_id,	
		other_instorage_code,	
		store_sn,	
		store_name,	
		status,	
		remark,	
		auditby,	
		audit_time,	
		createby,	
		create_time,	
		updateby,	
		update_time,
		type,
		pre_arrival_time,
		commit_time,
		commitby
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{other_instorage_id},
        #{other_instorage_code},	
        #{store_sn},	
        #{store_name},	
        #{status},	
        #{remark},	
        #{auditby},	
        #{audit_time},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time},	
        #{type},	
        #{pre_arrival_time}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        other_instorage_id,	
		        other_instorage_code,	
		        store_sn,	
		        store_name,	
		        status,	
		        remark,	
		        auditby,	
		        audit_time,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time,
		        type,
		        pre_arrival_time
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			other_instorage_id = #{other_instorage_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			<if test="store_sn!= null and  store_sn!= ''">
				store_sn = #{store_sn},
			</if>
			<if test="store_name != null and  store_name!= ''">
				store_name = #{store_name},
			</if>
			<if test="status != null and  status!= ''">
				status = #{status},
			</if>
			<if test="remark != null and  remark!= ''">
				 remark= #{remark},
			</if>
			<if test="other_instorage_code != null and  other_instorage_code!= ''">
				other_instorage_code = #{other_instorage_code},
			</if>
			<if test="auditby != null and  auditby!= ''">
				 auditby= #{auditby},
			</if>
			<if test="audit_time != null and  audit_time!= ''">
				 audit_time= #{audit_time},
			</if>
			<if test="update_time != null and  update_time!= ''">
				 update_time= #{update_time},
			</if>
			<if test="pre_arrival_time != null and  pre_arrival_time!= ''">
				 pre_arrival_time= #{pre_arrival_time},
			</if>
			<if test="type != null and  type!= ''">
				 type= #{type},
			</if>
			<if test="commit_time!= null">
			commit_time = #{commit_time},
		</if>
		<if test="commitby!= null">
			commitby = #{commitby},
		</if>
		        	
		other_instorage_id = other_instorage_id
		where 
		other_instorage_id = #{other_instorage_id}
	</update>
	
	<update id="updateStatus" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			<if test="status != null and  status!= ''">
				status = #{status},
			</if>
			<if test="update_time != null and  update_time!= ''">
				 update_time = #{update_time},
			</if>
			<if test="updateby != null and  updateby!= ''">
				 updateby = #{updateby},
			</if>
		other_instorage_code = other_instorage_code
		where 
		other_instorage_code = #{other_instorage_code}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			other_instorage_id = #{other_instorage_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		and store_sn in 
		<foreach item="item" index="index" collection="pd.my_store" open="(" separator="," close=")">  
		  #{pd.my_store[${index}]}
		</foreach> 
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		<if test="pd.store_sn!= null and pd.store_sn != ''">
			and 
			(
				store_sn = #{pd.store_sn}
			)
		</if>
		<if test="pd.other_instorage_code !=null and pd.other_instorage_code != ''">
			and 
			(
				other_instorage_code = #{pd.other_instorage_code}
			)
		</if>
		<if test="pd.status!= null and pd.status != ''">
			and 
			(
				status = #{pd.status}
			)
		</if>
		order by create_time desc
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		order by create_time desc
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			other_instorage_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<select id="findByCode" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			other_instorage_code = #{other_instorage_code}
	</select>
	
	<select id="querylistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		and store_sn in 
		<foreach item="item" index="index" collection="pd.my_store" open="(" separator="," close=")">  
		  #{pd.my_store[${index}]}
		</foreach> 
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		<if test="pd.store_sn!= null and pd.store_sn != ''">
			and 
			(
				store_sn = #{pd.store_sn}
			)
		</if>
		<if test="pd.other_instorage_code !=null and pd.other_instorage_code != ''">
			and 
			(
				other_instorage_code LIKE CONCAT(CONCAT('%', #{pd.other_instorage_code}),'%')
			)
		</if>
		<if test="pd.status!= null and pd.status != ''">
			and 
			(
				status = #{pd.status}
			)
		</if>
		order by create_time desc
	</select>
	<!-- fh313596790qq(青苔) -->
</mapper>