<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DeliveryOrgRegionMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_delivery_org_region
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		delivery_org_id,	
		region_code,	
		region_name,	
		status,	
		delivery_org_region_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{delivery_org_id},	
        #{region_code},	
        #{region_name},	
        #{status}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        delivery_org_id,	
		        region_code,	
		        region_name,	
		        status	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			delivery_org_region_id = #{delivery_org_region_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			delivery_org_id = #{delivery_org_id},
			region_code = #{region_code},
			region_name = #{region_name},
		delivery_org_region_id = delivery_org_region_id
		where 
		delivery_org_region_id = #{delivery_org_region_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			org.delivery_org_name,
			orgReg.delivery_org_id,	
			orgReg.region_code,	
			orgReg.region_name,	
			orgReg.status,	
			orgReg.delivery_org_region_id
		from 
			pgt_delivery_org_region orgReg left outer join	pgt_delivery_org org 
		on org.delivery_org_id= orgReg.delivery_org_id
		where 
			orgReg.delivery_org_region_id = #{delivery_org_region_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select 
			org.delivery_org_name,
			orgReg.delivery_org_id,	
			orgReg.region_code,	
			orgReg.region_name,	
			orgReg.status,	
			orgReg.delivery_org_region_id
		from 
			pgt_delivery_org_region orgReg left outer join	pgt_delivery_org org 
		on org.delivery_org_id= orgReg.delivery_org_id
		where 1=1
		<if test="pd.delivery_org_name!= null and pd.delivery_org_name != ''"><!-- 关键词检索 -->
			and org.delivery_org_name like concat('%', #{pd.delivery_org_name}, '%')
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select 
			org.delivery_org_name,
			orgReg.delivery_org_id,	
			orgReg.region_code,	
			orgReg.region_name,	
			orgReg.status,	
			orgReg.delivery_org_region_id
		from 
			pgt_delivery_org_region orgReg left outer join	pgt_delivery_org org 
		on org.delivery_org_id= orgReg.delivery_org_id
		where 
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			delivery_org_region_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>