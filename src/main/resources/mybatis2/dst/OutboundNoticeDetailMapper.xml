<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OutboundNoticeDetailMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_outbound_notice_detail
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		outbound_notice_code,	
		purchase_code,	
		purchase_detail_id,	
		sku_id,	
		sku_name,	
		pre_send_quantity,	
		send_quantity,	
		status,	
		createby,	
		create_time,	
		updateby,	
		update_time,	
		outbound_notice_detail_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{outbound_notice_code},	
        #{purchase_code},	
        #{purchase_detail_id},	
        #{sku_id},	
        #{sku_name},	
        #{pre_send_quantity},	
        #{send_quantity},	
        #{status},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd" useGeneratedKeys="true" keyProperty="outbound_notice_detail_id">
		insert into 
	<include refid="tableName"></include>
		(
		        outbound_notice_code,	
		        purchase_code,	
		        purchase_detail_id,	
		        sku_id,	
		        sku_name,	
		        pre_send_quantity,	
		        send_quantity,	
		        status,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			outbound_notice_detail_id = #{outbound_notice_detail_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			outbound_notice_code = #{outbound_notice_code},
			purchase_code = #{purchase_code},
			sku_name = #{sku_name},
			pre_send_quantity = #{pre_send_quantity},
			send_quantity = #{send_quantity},
			status = #{status},
			createby = #{createby},
			create_time = #{create_time},
			updateby = #{updateby},
			update_time = #{update_time},
		outbound_notice_detail_id = outbound_notice_detail_id
		where 
		outbound_notice_detail_id = #{outbound_notice_detail_id}
	</update>
	
	
	<!-- 修改数量 -->
	<update id="updateSendQuantity" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			send_quantity = #{send_quantity},
			updateby = #{updateby},
			update_time =NOW()
		where 
		outbound_notice_detail_id = #{outbound_notice_detail_id}
	</update>
	
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			outbound_notice_detail_id = #{outbound_notice_detail_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1 
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where outbound_notice_code = #{outbound_notice_code}
	</select>
	
	<select id="findAllByOutBoundNoticeCode" parameterType="pd" resultType="pd">
		
		SELECT 
			outbound_notice_detail_id,
			s.sku_barcode,
                outbound_notice_code,	
		purchase_code,	
		purchase_detail_id,	
		ond.sku_id,	
		ond.sku_name,	
		pre_send_quantity,	
		send_quantity,	
		STATUS,	
		outbound_notice_detail_id,
		s.sku_encode,
		g.isopen_batch,
		g.spec,g.unit_name,g.attr_cate,g.tax_rate
FROM pgt_outbound_notice_detail  ond INNER JOIN szy_goods_sku_6  s
 ON  ond.sku_id=s.sku_id
INNER JOIN  szy_goods_6 g ON g.goods_id=s.goods_id
		where outbound_notice_code = #{outbound_notice_code}
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			outbound_notice_detail_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>