<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DirectSalesCustomerMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_direct_sales_customer
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		direct_sales_code,	
		customer_id,	
		customer_name,	
		amount,	
		status,	
		createby,	
		create_time,	
		updateby,	
		update_time,	
		direct_sales_customer_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{direct_sales_code},	
        #{customer_id},	
        #{customer_name},	
        #{amount},	
        #{status},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        direct_sales_code,	
		        customer_id,	
		        customer_name,	
		        amount,	
		        status,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			direct_sales_customer_id = #{direct_sales_customer_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			direct_sales_code = #{direct_sales_code},
			customer_id = #{customer_id},
			customer_name = #{customer_name},
			amount = #{amount},
			updateby = #{updateby},
			update_time = #{update_time},
		direct_sales_customer_id = direct_sales_customer_id
		where 
		direct_sales_customer_id = #{direct_sales_customer_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			direct_sales_customer_id = #{direct_sales_customer_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		order by create_time desc
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		
		order by create_time desc
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			direct_sales_customer_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	<select id="listByCode" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="direct_sales_code!= null and direct_sales_code != ''"><!-- 关键词检索 -->
			and
			direct_sales_code = #{direct_sales_code}
		</if>
		
		order by create_time desc
	</select>
	
	<!-- 通过ID获取数据 -->
	<select id="queryCustomer" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			customer_id = #{customer_id}
		and
			direct_sales_code = #{direct_sales_code}
	</select>
	<!-- fh313596790qq(青苔) -->
</mapper>