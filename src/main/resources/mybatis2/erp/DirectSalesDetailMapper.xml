<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DirectSalesDetailMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_direct_sales_detail
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		direct_sales_code,	
		sku_id,	
		sku_name,
		sku_encode,	
		quantity,	
		unit,	
		price,	
		amount,	
		status,	
		createby,	
		create_time,	
		updateby,	
		update_time,	
		direct_sales_detail_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{direct_sales_code},	
        #{sku_id},	
        #{sku_name},
        #{sku_encode},	
        #{quantity},	
        #{unit},	
        #{price},	
        #{price}*#{quantity},
        #{status},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        direct_sales_code,	
		        sku_id,	
		        sku_name,
		        sku_encode,	
		        quantity,	
		        unit,	
		        price,	
		        amount,	
		        status,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			direct_sales_detail_id = #{direct_sales_detail_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			direct_sales_code = #{direct_sales_code},
			sku_id = #{sku_id},
			sku_name = #{sku_name},
			sku_encode = #{sku_encode},
			quantity = #{quantity},
			unit = #{unit},
			price = #{price},
			amount = #{price}*#{quantity},
			updateby = #{updateby},
			update_time = #{update_time},
		direct_sales_detail_id = direct_sales_detail_id
		where 
		direct_sales_detail_id = #{direct_sales_detail_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			direct_sales_detail_id = #{direct_sales_detail_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		
		order by create_time desc
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		
		order by create_time desc
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			direct_sales_detail_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	
	<!-- 列表 -->
	<select id="listByCode" parameterType="pd" resultType="pd">
	select
		pad.direct_sales_code direct_sales_code,	
		pad.sku_id sku_id,	
		pad.sku_name sku_name,
		pad.sku_encode skue,	
		pad.quantity quantity,	
		pad.price price,	
		pad.amount amount,	
		pad.status status,	
		pad.createby createby,	
		pad.create_time create_time,	
		pad.updateby updateby,	
		pad.update_time update_time,	
		pad.direct_sales_detail_id direct_sales_detail_id,
		t3.unit_name unit,
		g.spec,
		g.isopen_batch,
		g.attr_cate,
		gs.sku_encode sku_encode
		from pgt_direct_sales_detail pad
		left join szy_goods_sku_6 gs on pad.sku_id = gs.sku_id
		left join szy_goods_6 g on g.goods_id = gs.goods_id
		left join szy_goods_unit t3 on t3.unit_id = g.unit_id
		where 1=1
		<if test="direct_sales_code!= null and direct_sales_code != ''">
			and
			direct_sales_code = #{direct_sales_code}
		</if>
		
		order by create_time desc
	</select>
	<select id="listByCodeBak" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="direct_sales_code!= null and direct_sales_code != ''">
			and
			direct_sales_code = #{direct_sales_code}
		</if>
		
		order by create_time desc
	</select>
	<!-- 修改 -->
	<update id="updatePrice" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			
			price = #{price},
			amount = #{price}*quantity,
			updateby = #{updateby},
			update_time =NOW()

		where 
		direct_sales_detail_id = #{direct_sales_detail_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="queryGoodSku" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			direct_sales_code = #{direct_sales_code}
			and
			sku_id = #{sku_id}
	</select>
	<!-- fh313596790qq(青苔) -->
</mapper>