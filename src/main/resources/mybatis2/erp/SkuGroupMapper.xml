<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SkuGroupMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_sku_group
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		parent_sku_id,	
		sku_id,	
		quantity,	
		status,	
		createby,	
		create_time,	
		updateby,	
		update_time,	
		sku_group_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{parent_sku_id},	
        #{sku_id},	
        #{quantity},	
        #{status},	
        #{createby},	
        #{create_time},	
        #{updateby},	
        #{update_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        parent_sku_id,	
		        sku_id,	
		        quantity,	
		        status,	
		        createby,	
		        create_time,	
		        updateby,	
		        update_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			sku_group_id = #{sku_group_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			parent_sku_id = #{parent_sku_id},
			sku_id = #{sku_id},
			quantity = #{quantity},
			status = #{status},
			createby = #{createby},
			create_time = #{create_time},
			updateby = #{updateby},
			update_time = #{update_time},
		sku_group_id = sku_group_id
		where 
		sku_group_id = #{sku_group_id}
	</update>
	<update id="update" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			quantity = #{quantity},
			updateby = #{updateby},
			update_time = #{update_time},
		sku_group_id = sku_group_id
		where 
		sku_group_id = #{sku_group_id}
	</update>
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
			sg.parent_sku_id,	
			sg.sku_id,	
			sg.quantity,	
			sg.status,	
			sg.createby,	
			sg.create_time,	
			sg.updateby,	
			sg.update_time,	
			sg.sku_group_id,
			gs.sku_name,
			gs.sku_barcode,
			gs.sku_encode,
			gs.goods_price
		from 
			pgt_sku_group	sg
		inner join 
			szy_goods_sku_6 gs
		on 
			gs.sku_id=sg.sku_id
		where 
			sg.sku_group_id = #{sku_group_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			sku_group_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 列表(全部) -->
	<select id="listByParentId" parameterType="pd" resultType="pd">
		select
			sg.parent_sku_id,	
			sg.sku_id,	
			sg.quantity,	
			sg.status,	
			sg.createby,	
			sg.create_time,	
			sg.updateby,	
			sg.update_time,	
			sg.sku_group_id,
			gs.sku_name,
			gs.sku_barcode,
			gs.sku_encode,
			gs.goods_price
		from 
			pgt_sku_group	sg
		inner join 
			szy_goods_sku_6 gs
		on 
			gs.sku_id=sg.sku_id
		where
			sg.parent_sku_id = #{parent_sku_id}	 
	</select>
	<select id="findHas" parameterType="pd" resultType="pd">
		select
			sg.parent_sku_id,	
			sg.sku_id,	
			sg.quantity,	
			sg.status,	
			sg.createby,	
			sg.create_time,	
			sg.updateby,	
			sg.update_time,	
			sg.sku_group_id
		from 
			pgt_sku_group	sg

		where 
			sg.parent_sku_id = #{parent_sku_id}
		and
			sg.sku_id = #{sku_id}	
	</select>
	<select id="listBySkuId" parameterType="pd" resultType="pd">
		select
			sg.parent_sku_id,	
			sg.sku_id,	
			sg.quantity,	
			sg.status,	
			sg.createby,	
			sg.create_time,	
			sg.updateby,	
			sg.update_time,	
			sg.sku_group_id,
			gs.sku_name,
			gs.sku_barcode,
			gs.sku_encode,
			gs.goods_price
		from 
			pgt_sku_group	sg
		inner join 
			szy_goods_sku_6 gs
		on 
			gs.sku_id=sg.sku_id
		where
			sg.sku_id = #{sku_id}	 
	</select>
	<!-- fh313596790qq(青苔) -->
</mapper>