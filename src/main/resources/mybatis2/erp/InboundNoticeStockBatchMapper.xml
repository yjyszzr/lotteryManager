<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="InboundNoticeStockBatchMapper">
	
	<!--表名 -->
	<sql id="tableName">
		pgt_inbound_notice_stock_batch
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		inbound_notice_code,	
		notice_detail_id,	
		sku_id,	
		sku_name,	
		sku_encode,
		sku_barcode,	
		batch_code,	
		quantity,	
		spec,	
		unit_name,	
		status,
		inbound_notice_stock_batch_id
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{inbound_notice_code},	
        #{notice_detail_id},	
        #{sku_id},	
        #{sku_name},	
        #{sku_encode},	
        #{sku_barcode},	
        #{batch_code},	
        #{quantity},	
        #{spec},	
        #{unit_name},	
        #{status}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        inbound_notice_code,	
		        notice_detail_id,	
		        sku_id,	
		        sku_name,	
		        sku_encode,
		        sku_barcode,		
		        batch_code,	
		        quantity,	
		        spec,	
		        unit_name,
		        status,
		        createby,	
				create_time,	
				updateby,	
				update_time	
		) values (
				#{inbound_notice_code},	
		        #{notice_detail_id},	
		        #{sku_id},	
		        #{sku_name},	
		        #{sku_encode},	
		        #{sku_barcode},	
		        #{batch_code},	
		        #{quantity},	
		        #{spec},	
		        #{unit_name},
		        #{status},
		        #{createby},
		        #{create_time},
		        #{updateby},
		        #{update_time}
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			inbound_notice_stock_batch_id = #{inbound_notice_stock_batch_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			inbound_notice_code = #{inbound_notice_code},
			notice_detail_id = #{notice_detail_id},
			sku_id = #{sku_id},
			sku_name = #{sku_name},
			sku_encode = #{sku_encode},
			sku_barcode = #{sku_barcode},
			batch_code = #{batch_code},
			quantity = #{quantity},
			spec = #{spec},
			unit_name = #{unit_name},
			updateby = #{updateby},	
			update_time = #{update_time},
		inbound_notice_stock_batch_id = inbound_notice_stock_batch_id
		where 
		inbound_notice_stock_batch_id = #{inbound_notice_stock_batch_id}
	</update>
	
	<update id="updateStatus" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			status = #{status},
			<if test="batch_code!= null and batch_code != ''">
				batch_code = #{batch_code},
			</if>
			
			updateby = #{updateby},	
			update_time = #{update_time},
		inbound_notice_stock_batch_id = inbound_notice_stock_batch_id
		where 
		inbound_notice_stock_batch_id = #{inbound_notice_stock_batch_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			inbound_notice_stock_batch_id = #{inbound_notice_stock_batch_id}
	</select>
	<!-- 获取数据不能存在相同的批次 -->
	<select id="isHasBatch" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			notice_detail_id = #{notice_detail_id}
			and
			batch_code = #{batch_code}
			and
			sku_barcode = #{sku_barcode}
	</select>
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
		<if test="pd.status!= null and pd.status != ''">
			and  status=#{pd.status}
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="notice_detail_id != null and notice_detail_id != ''">
			and notice_detail_id = #{notice_detail_id}
		</if>
		
		 <if test="status!= null and status != ''">
			and status=#{status}
		</if> 
		 <if test="statusMin!= null and statusMin != ''">
			and status>=#{statusMin}
		</if> 
		<if test="inbound_notice_code != null and inbound_notice_code != ''">
			and inbound_notice_code=#{inbound_notice_code}
		</if>
	</select>
	
	<select id="listByPd" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="notice_detail_id != null and notice_detail_id != ''">
			and notice_detail_id = #{notice_detail_id}
		</if>
		<if test="inbound_notice_code != null and inbound_notice_code != ''">
			and inbound_notice_code=#{inbound_notice_code}
		</if>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			inbound_notice_stock_batch_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>