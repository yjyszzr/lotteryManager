<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ChannelConsumerMapper">
	
	<!--表名 -->
	<sql id="tableName">
		dl_channel_consumer
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		consumer_id,	
		channel_distributor_id,	
		user_id,	
		user_name,	
		mobile,	
		consumer_ip,	
		add_time,	
		device_code,	
		deleted,	
		frist_login_time
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
        #{consumer_id},	
        #{channel_distributor_id},	
        #{user_id},	
        #{user_name},	
        #{mobile},	
        #{consumer_ip},	
        #{add_time},	
        #{device_code},	
        #{deleted},	
        #{frist_login_time}	
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
		        consumer_id,	
		        channel_distributor_id,	
		        user_id,	
		        user_name,	
		        mobile,	
		        consumer_ip,	
		        add_time,	
		        device_code,	
		        deleted,	
		        frist_login_time	
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			consumer_id = #{consumer_id}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			user_name = #{user_name},
			mobile = #{mobile},
			add_time = #{add_time},
			deleted = #{deleted},
			frist_login_time = #{frist_login_time}
		where 
		consumer_id = #{consumer_id}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			consumer_id = #{consumer_id}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	<select id="findByChannelId"  parameterType="pd"  resultType="pd">
			SELECT
			consumer_id,
			channel_distributor_id,
			user_id,
			user_name,
			mobile,
			frist_login_time,
			date_format( FROM_UNIXTIME( add_time ), '%Y-%m-%d  %H:%i:%s' ) AS add_time,
			device_code,
			deleted
		FROM
			dl_channel_consumer 
		WHERE
			channel_distributor_id = #{distributorId}
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		SELECT
			* 
		FROM
			dl_channel_consumer 
		WHERE
			user_id IS NOT NULL
	</select>
	
	<select id="selectByTime" parameterType="String" resultType="pd">
		SELECT
			consumer_id,
			channel_distributor_id,
			user_id,
			user_name,
			mobile,
			frist_login_time,
			add_time 
		FROM
			dl_channel_consumer 
		WHERE
			! ISNULL( frist_login_time ) 
			AND date_format( FROM_UNIXTIME( frist_login_time ), '%Y-%m-%d %H:%i' ) = #{format}
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			consumer_id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
</mapper>